{
  "build": {
    "commands": {
      "server": "cd server && npm install"
    }
  },
  "deploy": {
    "startCommand": "cd server && npm start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
